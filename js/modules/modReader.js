class ModReader {
  constructor(file) {
    return {
      name: modName,
      author: modAuthor,
      description: modDesc,
      web_version: webVer,
      mod_version: modVer,
      mobileCompatible: mobile
    }
  }
}

export default ModReader;